import React from "react";
import ReactDOM from "react-dom";
import VersionsPanel from "./VersionsPanel";
import "./VersionsPanel.css"; // Import styles

ReactDOM.render(
  <React.StrictMode>
    <VersionsPanel />
  </React.StrictMode>,
  document.getElementById("root")
);